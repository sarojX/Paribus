version: "3.8"

services:
  web:
    build: .
    image: paribus:latest
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - HOSPITAL_API_BASE=${HOSPITAL_API_BASE:-https://hospital-directory.onrender.com}
    restart: unless-stopped
